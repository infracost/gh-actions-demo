on: [pull_request]
jobs:
  infracost:
    runs-on: ubuntu-latest
    env:
      working-directory: terraform
      INFRACOST_DASHBOARD_API_ENDPOINT: https://dashboard.api.dev.infracost.io
      INFRACOST_DASHBOARD_ENDPOINT: https://dashboard.dev.infracost.io
      INFRACOST_DEFAULT_PRICING_API_ENDPOINT: https://pricing.api.dev.infracost.io
      INFRACOST_PRICING_API_ENDPOINT: https://pricing.api.dev.infracost.io
      INFRACOST_ENABLE_DASHBOARD: true

    name: Run Infracost
    steps:
      - name: Setup Infracost
        run: |
          curl -o /tmp/infracost https://infracost-public-dumps.s3.amazonaws.com/builds/json/infracost-linux-amd64
          chmod +x /tmp/infracost
          /tmp/infracost configure set api_key ${{ secrets.INFRACOST_API_KEY_ALISTAIR_TEST }}

      - name: Checkout base branch
        uses: actions/checkout@v2
        with:
          ref: '${{ github.event.pull_request.base.ref }}'

      - name: Generate Infracost cost estimate baseline
        run: |
          /tmp/infracost breakdown --path=terraform \
                              --usage-file infracost-usage.yml \
                              --format=json \
                              --out-file=/tmp/infracost-base.json

      - name: Checkout PR branch
        uses: actions/checkout@v2

      - name: Run Infracost
        run: |
          /tmp/infracost diff --path=terraform \
                        --usage-file infracost-usage.yml \
                        --format=json \
                        --compare-to=/tmp/infracost-base.json \
                        --out-file=/tmp/infracost.json

      # See https://www.infracost.io/docs/features/cli_commands/#comment-on-pull-requests for other options.
      - name: Post Infracost comment
        run: |
          # Posts a comment to the PR using the 'update' behavior.
          # This creates a single comment and updates it. The "quietest" option.
          # The other valid behaviors are:
          #   delete-and-new - Delete previous comments and create a new one.
          #   hide-and-new - Minimize previous comments and create a new one.
          #   new - Create a new cost estimate comment on every push.
          /tmp/infracost comment github --path /tmp/infracost.json \
                                   --repo $GITHUB_REPOSITORY \
                                   --github-token ${{github.token}} \
                                   --pull-request ${{github.event.pull_request.number}} \
                                   --behavior update
                                   # --policy-path cost-policy.rego TODO add this back when OPA has fixed https://github.com/open-policy-agent/opa/issues/4610
